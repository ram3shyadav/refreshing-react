{
  "name": "micro",
  "private": true,
  "scripts": {
    "build": "yarn workspaces run build",
    "start": "wsrun --parallel start"
  },
  "workspaces": [
    "apps/*"
  ],
  "devDependencies": {
    "concurrently": "^7.6.0",
    "wsrun": "^5.2.4"
  }
}
